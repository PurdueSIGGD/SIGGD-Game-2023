using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiniMap : MonoBehaviour
{
    private GameObject radarRing;

    // Start is called before the first frame update
    void Start()
    {
        //radarRing = 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
